import { useState, useEffect } from 'react';
import { DIV } from './style';
import Link from 'next/link';
// 프론트에 점수를 다 저장하고 마지막 최종점수를 백엔드에 보내준다. 프론트 저장 : localstorage에 저장하기
// 선택지를 최종적으로 보내준다.
const Result = () => {
    return <>결과화면</>;
};

export default Result;
